apiVersion: v1
kind: Service
metadata:
  name: {{ include "uri-routing-example.fullname" . }}
  labels:
    {{- include "uri-routing-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uri-routing-example.selectorLabels" . | nindent 4 }}
